{
  "name": "chat-bot",
  "version": "1.0.0",
  "description": "Real-time chat with WebSockets at scale — Socket.IO + Redis pub/sub across multiple server instances, with a built-in AI chatbot.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "@socket.io/redis-adapter": "^8.3.0",
    "dompurify": "^3.4.11",
    "express": "^4.21.2",
    "marked": "^18.0.5",
    "redis": "^4.7.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1"
  }
}
